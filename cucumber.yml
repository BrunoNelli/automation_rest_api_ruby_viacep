# Plataform
default: -p json_report -p pretty -p html

# Expõe o log do HTTParty no console
api_log: -p http_log

# Reports
json_report: --format pretty --out=reports/rubocop.html.json

# Logs
http_log: HTTP_LOG=true

pretty: --format pretty
html: --format html --out=reports/relatorio.html
